# 09: Masonry notes layout

**What to build:** The `/notes` grid uses a Google Keep-style masonry layout with variable-height cards instead of a uniform row grid. Pinned-first and label-filter behaviour from earlier tickets still works.

**Blocked by:** 03 Note metadata — color, pin, archive

**Status:** done

- [x] Notes on `/notes` render in masonry columns (CSS columns, flex masonry, or lightweight approach — no heavy dependency unless justified)
- [x] Variable-height cards (checklist notes, long body) layout cleanly
- [x] Responsive: usable on phone width and kiosk laptop width
- [x] Pin order and label filter still behave correctly in masonry layout
